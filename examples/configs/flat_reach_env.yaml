world:
    type: FlatReachingWorld
    spec:
        friction: 0
        reaching_eps: 1
        agent_goal_lists:
            # robot: [[10, 15]]
            # human: [[30.0, 20.0]]
            # 'random'/'static'/'near'/explicit
            robot: 'random'
            # Q 1.1
            human: 'static'
            # Q 1.2
            # human: 'near'
dt: 0.02
agent_comp_spec:
    robot: configs/flat_reach_agent_1.yaml
    human: configs/flat_reach_agent_2.yaml
agent_env_spec:

    robot:
        type: BB8Agent
        spec:
            # for specified location
            # init_x: [50.0, 20.0, 0.0, 0.0]
            # for random location
            init_x: null
            u_max: [1000, 1000]

    human:
        type: BB8Agent
        spec:
            # init_x: [30.0, 20.0, 0.0, 0.0]
            init_x: null

